# deploy/compose

Reserved for the supported self-hosted Docker Compose deployment
(see `docs/architecture.md`, "Deployment → Self-hosted"). Milestone 00 ships
the local-development compose at the repository root (`docker-compose.yml`)
and an optional app-image wiring (`docker-compose.app.yml`); the supported
self-hosted stack lands in a later milestone.
