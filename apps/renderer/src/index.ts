const message = {
  level: "info",
  service: "netrics-renderer",
  role: "renderer",
  msg: "renderer placeholder: the Playwright snapshot worker ships in a later milestone",
};

console.log(JSON.stringify(message));
